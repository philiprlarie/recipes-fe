{{input-searchable
  label="Ingredient name"
  placeholder="Ingredient name"
  createButtonText="Create ingredient with name"
  getSearchResults=getSearchResults
  optionWasSelected=(action ingredientWasSelected)
  createOptionWasSelected=(action "createIngredientWasSelected")
  selectedOption=ingredient
  keyPath="name"
  validationErrorMessages=validationErrorMessages
  formWasSubmitted=formWasSubmitted
}}
